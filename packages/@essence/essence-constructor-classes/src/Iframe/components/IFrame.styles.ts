import {makeStyles} from "@essence-community/constructor-share";

export const useStyles = makeStyles(
    () => ({
        iframe: {
            border: "none",
            width: "100%",
        },
    }),
    {
        name: "EssenceIFrame",
    },
);
