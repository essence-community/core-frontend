const BuilderWindowStylesLight = (theme) => ({
    addMoreLabelColor: {
        color: theme.palette.text.primary,
    },
    iconColor: {
        color: theme.palette.primary.field,
    },
});

export default BuilderWindowStylesLight;
