import {makeStyles} from "@essence-community/constructor-share";

export const useStyles = makeStyles(
    () => ({
        paper: {
            width: "100%",
        },
    }),
    {name: "EssenceHistoryPanelContainer"},
);
