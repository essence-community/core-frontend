/* eslint-disable sort-keys */
/* eslint-disable @typescript-eslint/camelcase */
/*
 * Import * as React from "react";
 *import {Icon} from "@essence-community/constructor-share/Icon";
 *import {getBaseBc, Renderer} from "@essence-community/constructor-share/utils/test";
 *import {shallow} from "enzyme";
 *import {TextFieldLabel} from "@essence-community/constructor-share/uicomponents/TextFieldLabel";
 *import {FieldCheckboxContainer} from "../containers/FieldCheckboxContainer";
 */
// eslint-disable-next-line max-lines-per-function
describe("FieldCheckbox", () => {
    /*
     * Const bcPanel = getBaseBc("PANEL", {
     *  type: "PANEL",
     *  childs: [
     *      {
     *          type: "IFIELD",
     *          cv_name: "field checkbox",
     *          cn_order: 1,
     *          datatype: "checkbox",
     *          ck_object: "69EFB01384A046D29EC08E2D1A57EDAB",
     *          ck_parent: "3C49441A356447F88B1C425C49A115BC",
     *          cl_dataset: 0,
     *          cv_displayed: "541c221f83014cd184aac1201df310b0",
     *          ck_page_object: "D3AE10D64D1342E39F0DA5A07AFC7DFE",
     *          cv_description: "field checkbox",
     *          visibleinwindow: "false",
     *      },
     *  ],
     *  reqsel: "true",
     *  cv_name: "Test Panel",
     *  cn_order: 20,
     *  ck_modify: "modify",
     *  ck_object: "10713CC844CC4BFBA032B412494688FF",
     *  resizable: "false",
     *  cl_dataset: 1,
     *  idproperty: "ck_id",
     *  contentview: "hbox",
     *  cv_displayed: "0fe81269c651472c86f5f19e8bcec652",
     *  ck_page_object: "3C49441A356447F88B1C425C49A115BC",
     *  cv_description: "Test Panel",
     *  getmastervalue: "ck_id",
     *  cv_helper_color: "red",
     *} as any);
     *
     *it("render", () => {
     *  const wrapper = shallow(<Renderer bc={bc} component={FieldCheckboxContainer} />);
     *
     *  expect(wrapper.exists()).toBeTruthy();
     *  expect(
     *      wrapper
     *          .find("label")
     *          .at(0)
     *          .prop("data-qtip"),
     *  ).toBe("Нет");
     *  expect(wrapper.find(TextFieldLabel).length).toBe(1);
     *  expect(wrapper.find(Icon).prop("iconfont")).toBe("square-o");
     *});
     *
     *it("value = true", () => {
     *  const wrapper = shallow(<Renderer bc={bc} component={FieldCheckboxContainer} />);
     *
     *  expect(
     *      wrapper
     *          .find("label")
     *          .at(0)
     *          .prop("data-qtip"),
     *  ).toBe("Да");
     *  expect(wrapper.find(Icon).prop("iconfont")).toBe("check-square");
     *});
     *
     *it("Не выводить label", () => {
     *  const wrapper = shallow(<Renderer bc={bc} noLabel component={FieldCheckboxContainer} />);
     *
     *  expect(wrapper.find(TextFieldLabel).length).toBe(0);
     *});
     *
     *it("При блокировки выводить закрашенный квардрат", () => {
     *  const wrapper = shallow(<Renderer bc={bc} disabled component={FieldCheckboxContainer} />);
     *
     *  expect(wrapper.find(Icon).prop("iconfont")).toBe("square");
     *});
     */
});
