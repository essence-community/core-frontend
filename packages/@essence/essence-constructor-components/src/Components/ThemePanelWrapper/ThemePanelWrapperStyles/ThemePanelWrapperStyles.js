const ThemePanelWrapperStyles = (theme) => ({
    contentEditing: {
        backgroundColor: theme.palette.common.white,
    },
    panelEditing: {
        position: "relative",
        zIndex: 3,
    },
});

export default ThemePanelWrapperStyles;
