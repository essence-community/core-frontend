// @flow
export const InlineButtonsStylesLight = (theme: any) => ({
    label: {
        alignSelf: "center",
        color: theme.palette.text.primary,
        position: "absolute",
        right: 50,
    },
});
