import {makeStyles} from "@essence-community/constructor-share";

export const useStyles = makeStyles(
    () => ({
        root: {
            padding: "10px 0",
        },
    }),
    {
        name: "FavoritePages",
    },
);
