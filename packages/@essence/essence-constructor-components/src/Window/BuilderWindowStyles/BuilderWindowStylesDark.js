const BuilderWindowStylesDark = (theme) => ({
    addMoreLabelColor: {
        color: theme.palette.common.white,
    },
    iconColor: {
        color: theme.palette.common.white,
    },
});

export default BuilderWindowStylesDark;
