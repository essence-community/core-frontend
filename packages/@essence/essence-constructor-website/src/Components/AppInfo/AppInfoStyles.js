const AppInfoStyles = (theme) => ({
    attention: {
        fontSize: 13,
        fontWeight: 700,
        textTransform: "uppercase",
    },
    button: {
        height: theme.sizing.appbarHeight,
        minWidth: theme.sizing.appbarHeight,
        width: theme.sizing.appbarHeight,
    },
    dialogPaper: {
        maxWidth: 520,
    },
    title: {
        fontSize: 26,
        fontWeight: 700,
    },
});

export default AppInfoStyles;
